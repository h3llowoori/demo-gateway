spring:
  application:
    name: book-api

server:
  port: 9001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
    registerWithEureka: true
    fetch-registry: true