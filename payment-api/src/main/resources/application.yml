spring:
  application:
    name: payment-api

server:
  port: 9002

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
    registerWithEureka: true
    fetch-registry: true